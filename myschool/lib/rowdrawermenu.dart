import 'package:flutter/material.dart';

class RowDrawerMenu extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new InkWell(
      child: new Column(
        children: <Widget>[

        ],
      ),
    );
  }

}
